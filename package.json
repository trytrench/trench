{
  "private": true,
  "globalDependencies": [
    "**/.env"
  ],
  "scripts": {
    "build": "dotenv -- turbo run build",
    "dev": "dotenv -- turbo run dev",
    "start": "dotenv -- turbo run start",
    "db:reset": "dotenv -- turbo run db:reset",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "generate": "turbo run generate",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "dotenv-cli": "latest",
    "eslint": "^8.48.0",
    "prettier": "^3.0.3",
    "turbo": "1.10.4"
  },
  "packageManager": "pnpm@8.8.0",
  "pnpm": {
    "patchedDependencies": {
      "sqrl-redis-functions@0.6.10": "patches/sqrl-redis-functions@0.6.10.patch"
    }
  }
}
